spring:
  application:
    name: Produtos Api

  datasource:
    url: jdbc:h2:mem:produtos
    username: sa
    password: deus

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console